# Core
python-dotenv
pydantic>=2
pyyaml

# Data / ML
numpy
pandas
scikit-learn
xgboost

# Storage
sqlalchemy>=2

# Dashboard (FastAPI + vanilla HTML/CSS/JS frontend in frontend/)
fastapi>=0.110
uvicorn[standard]>=0.29

# Razorpay integration
razorpay

# Local LLM (Ollama HTTP API) + LLM-judge client (Groq, OpenAI-compatible endpoint)
requests
openai

# LLM / fine-tuning (installed in Phase 3/7 — heavy, keep out of the base dev env if not yet needed)
# transformers
# peft
# trl
# bitsandbytes
# unsloth
# outlines

# Testing
pytest
