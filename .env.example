RETRY_EXECUTOR=stub
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
MODEL_BACKEND=prompted
OLLAMA_MODEL=qwen2.5:3b
OLLAMA_URL=http://localhost:11434

# LLM-judge for the escalation rubric eval (scripts/run_escalation_rubric_eval.py).
# Judge must be materially stronger than the qwen2.5:3b agent under test — using
# Groq's qwen/qwen3.8-27b (see evaluation/experiments/run_escalation_rubric_eval.py).
# Get a key at https://console.groq.com -> API Keys -> Create Key.
GROQ_API_KEY=
